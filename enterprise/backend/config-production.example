# Maternar Santa Mariense - Production Configuration Example
# IMPORTANTE: Copie este arquivo para .env quando for fazer deploy em produção

# Application
NODE_ENV=production
PORT=4000
HOST=0.0.0.0
APP_VERSION=2.0.0

# Database - PostgreSQL
# ⚠️ SUBSTITUA COM SUAS CREDENCIAIS REAIS DE PRODUÇÃO
DATABASE_URL=postgresql://maternar_user:SENHA_SEGURA_AQUI@production-db-host:5432/maternar_sm_prod

# Redis Cache
# ⚠️ SUBSTITUA COM SEU REDIS DE PRODUÇÃO
REDIS_URL=redis://:SENHA_REDIS_AQUI@production-redis-host:6379

# JWT Authentication
# 🔐 GERE UM SECRET FORTE E ÚNICO
# Exemplo de geração: openssl rand -base64 64
JWT_SECRET=GERAR_SECRET_FORTE_AQUI_USE_OPENSSL_RAND_BASE64_64
JWT_EXPIRES_IN=7d
JWT_REFRESH_EXPIRES_IN=30d

# CORS Configuration
# ⚠️ SUBSTITUA COM SEUS DOMÍNIOS REAIS
CORS_ORIGINS=https://maternarsm.com.br,https://app.maternarsm.com.br

# Security
ENABLE_RATE_LIMITING=true
RATE_LIMIT_WINDOW=900000
RATE_LIMIT_MAX=100

# Email Configuration (Produção)
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_USER=seu_usuario_smtp
SMTP_PASS=sua_senha_smtp

# Upload Configuration
UPLOAD_MAX_SIZE=10485760
UPLOAD_ALLOWED_TYPES=image/jpeg,image/png,image/gif,application/pdf

# Monitoring - Sentry
SENTRY_DSN=https://sua_chave_sentry@o123456.ingest.sentry.io/789012

# AWS Configuration (se usar S3 para uploads)
AWS_ACCESS_KEY_ID=sua_access_key
AWS_SECRET_ACCESS_KEY=sua_secret_key
AWS_REGION=us-east-1
AWS_S3_BUCKET=maternar-sm-uploads

# IMPORTANTE: Nunca commite este arquivo com valores reais!

